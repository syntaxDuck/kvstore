# Project Agents Configuration

This project uses **uv** for Python package management and running scripts.

## Commands

- Use `uv run <command>` instead of raw `<command>` (e.g., `uv run pytest`, `uv run python main.py`)
- Use `uv sync` to install dependencies
- Use `uv pip install <package>` to add dependencies
